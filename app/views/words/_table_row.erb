<tr>
  <td><%= word.name %></td>
  <% if can?(:manage, @word) %>
    <td><%= link_to 'Show', word %></td>
    <td><%= link_to 'Update', edit_word_path(word) %></td>
    <td><%= link_to 'Delete', word, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  <%end%>
</tr>