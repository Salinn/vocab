<tr id="word_video_<%= word_video.id %>">
  <td><%= word_video.video_description %></td>
  <td>
    <!-- ID doesn't matter here -->
    <%= link_to 'Delete', course_lesson_lesson_word_lesson_word_video_path(word_video_id: word_video.id, lesson_word_id: lesson_word.id, id: '1', course_id: @course.id, lesson_id: @lesson.id), remote: true, method: :delete, class: 'btn btn-sm btn-danger', data: { toggle: "confirmation", placement: "top" } %>
  </td>
</tr>