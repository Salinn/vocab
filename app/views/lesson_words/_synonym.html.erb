<tr id="synonym_<%= synonym.id %>">
  <td><%= synonym.word_synonym %></td>
  <td>
    <!-- ID doesn't matter here -->
    <%= link_to 'Delete', course_lesson_lesson_word_lesson_word_synonym_path(synonym_id: synonym.id, lesson_word_id: lesson_word.id, id: '1', course_id: @course.id, lesson_id: @lesson.id), remote: true, method: :delete, class: 'btn btn-sm btn-danger', data: { toggle: "confirmation", placement: "top" } %>
  </td>
</tr>