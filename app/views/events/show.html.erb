<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @event.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @event.description %>
</p>

<p>
  <strong>Start time:</strong>
  <%= @event.start_time %>
</p>

<p>
  <strong>End time:</strong>
  <%= @event.end_time %>
</p>

<%= link_to 'Back', course_events_path(course_id: @course.id), class:"btn btn-primary" %>
<% if can?(:manage, @course) %>
  <%= link_to 'Edit', edit_course_event_path(@event, course_id: @course.id), class:"btn btn-primary" %>
<% end %>


