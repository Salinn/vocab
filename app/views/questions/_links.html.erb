<% skip_or_next = ( correct || wrong ) ? 'Next Question' : 'Skip Question' %>
<%= link_to 'Previous Question', course_lesson_lesson_module_question_back_path(course_id: @course.id,lesson_id: @lesson_module.lesson.id,lesson_module_id: @lesson_module.id,question_id: @question.id), class: 'btn btn-primary', id: 'skip_or_back' %>
<%= link_to skip_or_next, course_lesson_lesson_module_question_skip_path(course_id: @course.id,lesson_id: @lesson_module.lesson.id,lesson_module_id: @lesson_module.id,question_id: @question.id), class: 'btn btn-primary', id: 'skip_or_next' %>
<br><br>
<%= link_to 'Back to Lessons', course_lessons_path(course_id: @course.id), class: 'btn btn-primary', id: 'back_to_lessons' %>
<%= link_to "View #{@question.lesson_module.name} Grades", course_lesson_lesson_module_path(@question.lesson_module, course_id: @course.id, lesson_id: @question.lesson_module.lesson.id), class: 'btn btn-primary', id: 'back_to_lessons' %>
